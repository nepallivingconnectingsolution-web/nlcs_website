# Base URL for the API.
# In development you can leave this empty to use Vite's proxy (/api -> :5000).
# In production set it to your deployed API origin, e.g. https://api.nlcsitservice.com
VITE_API_URL=
